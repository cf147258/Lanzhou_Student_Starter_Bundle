# Evidence workspace

Generated daily packs live here and are ignored by Git. `make check DAY=N
TEAM=Gx` creates the correct folder and starter files without overwriting work.

The tracked files under `templates/` define the minimum evidence shape. Raw
CSV, JSON, JSON Lines, and logs are evidence; screenshots alone are not.
